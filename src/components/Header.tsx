import Navigation from './Navigation';

const Header = () => {
  return (
    <header>
      <span className="main-header">ASYNC RACE</span>
      <Navigation />
    </header>
  );
};

export default Header;
